import { Component, EventEmitter, Input, Output } from '@angular/core';

@Component({
    selector: 'app-teste',
    template: `
      <span class="span">
        valorSpan
      </span>`
  })
  export class TesteComponent {
  }